Counter clicked ${{ counter }} ${'counter, "time || times"'}:
  Счетчик нажат ${{ counter }} ${'counter, "раз || раза || раз", "ru"'}
Today is ${() => i18n.formatDate(new Date())}:
  Сегодня ${() => i18n.formatDate(new Date())}

Increment: 'Увеличить'
Decrement: 'Уменьшить'
